# Minimal requirements for fallback scenarios
# Most dependencies are now handled via conda (see environment.yml)

# Only include packages that might not be available via conda-forge
# or are needed for specific Read the Docs functionality

# No additional pip requirements currently needed
# HOOMD-blue and all documentation dependencies are installed via conda